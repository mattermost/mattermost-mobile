# Multi-device message sync (MM-T3055 / MM-T3056).
# Half-flows under flows/multi_device/ stay excluded from dispatch; this
# scenario runs the orchestrator with DEVICE_A_UDID + DEVICE_B_UDID.
name: Multi-device message sync (MM-T3055, MM-T3056)
script: detox/maestro/scripts/run_two_device.sh
tags:
  - multi_device
