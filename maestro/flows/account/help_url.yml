# MM-T3260: Tapping Help in Settings opens the help URL in a browser.
#
# PRE-CONDITIONS:
#   - Logged-in test user (via utils/login.yml)
#
# REQUIRED ENV VARS:
#   - MAESTRO_APP_ID, TEST_USER_EMAIL, TEST_USER_PASSWORD
#
# ASSERTIONS:
#   - Help opens an external browser or in-app browser sheet
#   - iOS: breadcrumb or Done dismisses back to Settings with Help visible again
#   - Android: Back from Chrome Custom Tab returns to Settings with Help visible again
#
# testIDs:
#   - tab_bar.account.tab
#   - account.screen
#   - account.settings.option
#   - settings.help.option
#   - breadcrumb
tags:
  - MM-T3260
appId: ${MAESTRO_APP_ID}
---
- runFlow: ../../utils/login.yml

- tapOn:
    id: "tab_bar.account.tab"
- extendedWaitUntil:
    visible:
      id: "account.screen"
    timeout: 10000

- tapOn:
    id: "account.settings.option"
- extendedWaitUntil:
    visible:
      id: "settings.help.option"
    timeout: 10000

- tapOn:
    id: "settings.help.option"

# iOS: dismiss SFSafariViewController (same branches as attach_logs.yml).
- runFlow:
    when:
      platform: iOS
    commands:
      - runFlow:
          when:
            visible:
              id: "breadcrumb"
          commands:
            - takeScreenshot: help-browser-opened
            - tapOn:
                id: "breadcrumb"
      - runFlow:
          when:
            visible:
              text: "Done"
          commands:
            - takeScreenshot: help-browser-opened
            - tapOn:
                text: "Done"

# Android: Chrome Custom Tab — dismiss ads/FRE overlays before Back.
- runFlow:
    when:
      platform: Android
    commands:
      - extendedWaitUntil:
          notVisible:
            id: "settings.help.option"
          timeout: 8000
      - runFlow:
          file: ../../utils/dismiss_chrome_interstitial.yml
      - takeScreenshot: help-browser-opened
      - pressKey: Back
      - runFlow:
          file: ../../utils/dismiss_chrome_interstitial.yml
      - pressKey: Back

# iOS fallback: if browser wedged the session, relaunch and reopen Settings.
- runFlow:
    when:
      platform: iOS
      notVisible:
        id: "settings.help.option"
    commands:
      - launchApp:
          stopApp: true
          clearState: false
      - waitForAnimationToEnd:
          timeout: 30000
      - extendedWaitUntil:
          visible:
            id: "tab_bar.home.tab"
          timeout: 30000
      - tapOn:
          id: "tab_bar.account.tab"
      - extendedWaitUntil:
          visible:
            id: "account.settings.option"
          timeout: 10000
      - tapOn:
          id: "account.settings.option"

- extendedWaitUntil:
    visible:
      id: "settings.help.option"
    timeout: 10000
- takeScreenshot: help-browser-dismissed
