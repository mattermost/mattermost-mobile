# MM-T1325: Timezone display settings reflect the device timezone after app restart.
#
# PRE-CONDITIONS:
#   - Simulator/emulator timezone set to America/New_York before flow (CI pre-step)
#   - App launched with stopApp so native TZ is picked up at startup
#
# REQUIRED ENV VARS:
#   - MAESTRO_APP_ID, TEST_USER_EMAIL, TEST_USER_PASSWORD
#   - SIMULATOR_TIMEZONE (flow env block, default America/New_York)
#
# ASSERTIONS:
#   - Timezone settings screen shows automatic timezone option
#   - Region label displays "New York" for America/New_York
#
# testIDs:
#   - tab_bar.account.tab
#   - account.screen
#   - account.settings.option
#   - settings.display.option
#   - display_settings.screen
#   - display_settings.timezone.option
#   - timezone_display_settings.screen
#   - timezone_display_settings.automatic.option
tags:
  - MM-T1325
appId: ${MAESTRO_APP_ID}
env:
  SIMULATOR_TIMEZONE: America/New_York
---
# Step 1: Launch app fresh so native timezone is registered at startup
# (Simulator timezone must be set externally before this step — see PRE-REQUISITE above)
- launchApp:
    clearState: false
    stopApp: true

# Step 2: Login and navigate to timezone display settings
- runFlow: ../../utils/login.yml

# Step 3: Open Account settings
- tapOn:
    id: "tab_bar.account.tab"
- extendedWaitUntil:
    visible:
      id: "account.screen"
    timeout: 10000

# Step 4: Open Settings
- tapOn:
    id: "account.settings.option"
- extendedWaitUntil:
    visible:
      id: "settings.display.option"
    timeout: 10000

# Step 5: Open Display settings
# Note: SettingContainer appends ".screen" to all testIDs (see app/components/settings/container.tsx)
- tapOn:
    id: "settings.display.option"
- extendedWaitUntil:
    visible:
      id: "display_settings.screen"
    timeout: 10000

# Step 6: Open Timezone settings
- tapOn:
    id: "display_settings.timezone.option"
- extendedWaitUntil:
    visible:
      id: "timezone_display_settings.screen"
    timeout: 10000

# Step 7: Verify the timezone controls are present and show the correct timezone.
# autoUpdateTimezone fires on home screen load; allow time for the label to update on CI.
- assertVisible:
    id: "timezone_display_settings.automatic.option"
- runFlow:
    when:
      visible:
        text: "Off"
    commands:
      - tapOn:
          id: "timezone_display_settings.automatic.option"
- extendedWaitUntil:
    visible:
      text: "New York"
    timeout: 60000
- takeScreenshot: timezone-settings-visible
